<Window x:Class="MerHost.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PHP Ayarı" Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0D1117">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="PromptText" Grid.Row="0" Text="Değer girin:" Foreground="White" FontSize="14" Margin="0,0,0,10"/>
        
        <TextBox x:Name="InputTextBox" Grid.Row="1" Background="#21262D" Foreground="White" BorderBrush="#30363D" Padding="10,8" FontSize="14"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="İptal" Click="Cancel_Click" Padding="20,8" Background="#424242" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button Content="Tamam" Click="Ok_Click" Padding="20,8" Margin="10,0,0,0" Background="#238636" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
